<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>AI Clinical Insights</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

<style>
body {
  margin: 0;
  background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
  font-family: 'Poppins', sans-serif;
  color: white;
}

.container {
  max-width: 1100px;
  margin: auto;
  padding: 40px;
}

.glass {
  background: rgba(255,255,255,0.08);
  backdrop-filter: blur(14px);
  border-radius: 20px;
  padding: 30px;
  box-shadow: 0 0 40px rgba(255,165,0,0.25);
  animation: fadeIn 0.8s ease;
}

.score {
  font-size: 60px;
  font-weight: bold;
  text-align: center;
  color: <%= score > 80 ? "#00ffcc" : score > 60 ? "orange" : "red" %>;
  
}

.card {
  background: rgba(255,255,255,0.08);
  border-radius: 15px;
  padding: 20px;
  margin-top: 20px;
}

ul {
  list-style: none;
  padding: 0;
}

li {
  padding: 8px 0;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
</style>
</head>

<body>

<div class="container">
  <div class="glass">

    <h2>🧠 AI Clinical Analysis</h2>
    <p><b>Patient:</b> <%= patient.name %></p>

    <div class="score"><%= score %> / 100</div>
    <p style="text-align:center;">Overall Health Score</p>

    <div class="card">
      <h3>⚠️ Risk Factors</h3>
      <ul>
        <% risks.forEach(r => { %>
          <li>• <%= r %></li>
        <% }) %>
      </ul>
    </div>

    <div class="card">
      <h3>💡 Suggestions</h3>
      <ul>
        <% suggestions.forEach(s => { %>
          <li>• <%= s %></li>
        <% }) %>
      </ul>
    </div>

  </div>
</div>

</body>
</html>
